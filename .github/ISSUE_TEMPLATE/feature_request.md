---
name: Feature request
about: Propose a widget, a layout rule, a driver, or an API.
title: "feat: "
labels: enhancement
---

<!-- Sections marked (required) must be filled in. -->

**What it is** (required)

<!-- A widget, a layout rule, a terminal capability, a driver, or a change to an
     existing API. -->

**Problem** (required)

**Prior art**

<!-- Textual is what this library is written after: naming the file or the class
     there (for example `widgets/_tabs.py`) pins the semantics down and saves a
     round of guessing. A different toolkit is fine too — say which. -->

**Where it belongs**

- [ ] pure logic (runs on all four backends)
- [ ] a driver (touches a platform)
- [ ] undecided

**How it would be tested**

<!-- Pure logic is snapshot-tested with the headless driver. A driver is tested
     in CI, since the terminal it needs is not available locally. -->

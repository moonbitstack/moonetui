// What the terminal tells the program: keys, mouse reports, pasted text, size
// changes and focus. These are plain values with no behaviour, so a widget's
// reaction to a sequence of them can be written down and compared.

// The terminal's byte stream turned into events.
//
// One parser serves every platform: Windows is put into virtual terminal input
// mode, which makes its console send the same escape sequences a Unix terminal
// does, so there is no second code path to keep in step with this one.
import {
  "moonbitstack/moonetui/event",
}
